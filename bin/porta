#!/usr/bin/env ruby
# frozen_string_literal: true

require 'bundler/setup'
require 'iamspe'

# Moficar ARGV para substituir 'chefia' por 'porta'
ARGV.delete 'chefia'
ARGV.push('porta') unless ARGV.include?('porta')

# Iniciar o comando apropriado
cmd = Iamspe::Starter.new
cmd.parse(ARGV)
cmd.run
